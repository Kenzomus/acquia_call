Module Important Information safety.
the purpose is to expose important safety information (a node) as a resource in json.
 The endpoint = base_url /export .
Don't need to install it , if you already using the core webservice rest_ui cause you can expose your node resources by enabling in the configuration. 

